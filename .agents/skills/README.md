# Project-local skills

The `openrouter-video-builder` plugin in the parent OPENROUTER workspace is the procedure SSOT
for orchestration, evidence gates, implementation, and verification. It is intentionally not
copied into this repository.

Add a repository-local skill only when it owns a genuinely repository-specific procedure that
the project plugin does not already govern.
